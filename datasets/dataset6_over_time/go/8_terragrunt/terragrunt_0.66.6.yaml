package:
  name: terragrunt
  version: 0.66.6
  epoch: 0
  description: Thin wrapper for Terraform providing extra tools
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - terraform

environment:
  contents:
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
      - ./melange.rsa.pub
    repositories:
      - https://packages.wolfi.dev/os
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go
      - mockery

pipeline:
  - uses: git-checkout
    with:
      expected-commit: e0c5cbe815914aea348ff6d6fb0cf936c687e982
      repository: https://github.com/tdunlap607/terragrunt
      tag: v0.66.6

  - runs: go generate ./...

  - uses: go/build
    with:
      output: terragrunt
      packages: .
      ldflags: "-X  github.com/gruntwork-io/go-commons/version.Version=v${{package.version}}"

  - uses: strip

test:
  pipeline:
    - name: Verify terragrunt
      runs: |
        set -o pipefail
        terragrunt --version | grep "${{package.version}}"

update:
  enabled: true
  github:
    identifier: tdunlap607/terragrunt
    strip-prefix: v
    use-tag: true
    tag-filter: v